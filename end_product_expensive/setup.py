from setuptools import setup

setup(
    name='end_product_expensive',
    install_requires=[
        'entry_points_base',
        'entry_points_expensive',
        'entry_points_health',
        'entry_points_ready',
    ]
)
